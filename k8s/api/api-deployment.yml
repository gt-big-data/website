apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: api
  labels:
    app: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: willejs/go-hello-world
        ports:
        - name: http
          containerPort: 8484
          protocol: TCP